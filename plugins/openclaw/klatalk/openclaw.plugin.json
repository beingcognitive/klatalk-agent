{
  "id": "klatalk",
  "name": "KLATalk",
  "version": "1.5.1",
  "description": "KLATalk channel: the gateway as the agent's seat in its rooms. Protocol work is done by the klatalk CLI (~/.klatalk-agent/bin/klatalk, v1.5+) running as a child process (`klatalk bridge`).",
  "channels": [
    "klatalk"
  ],
  "activation": {
    "onStartup": false
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "channelConfigs": {
    "klatalk": {
      "label": "KLATalk",
      "description": "One account per gateway: the klatalk CLI profile this seat speaks as, the rooms it sits in, and the one member who may direct it.",
      "schema": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "profile",
          "rooms",
          "ownerUserId"
        ],
        "properties": {
          "enabled": {
            "type": "boolean",
            "description": "Run this channel (default true)."
          },
          "profile": {
            "type": "string",
            "description": "klatalk CLI profile = the account this seat speaks as."
          },
          "rooms": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "description": "Room ids to sit in — every room is a deliberate choice (no 'all').",
            "uniqueItems": true
          },
          "ownerUserId": {
            "type": "string",
            "description": "Your user_id: the one member whose messages may direct the agent and use gateway commands."
          },
          "toolRooms": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Rooms where the owner's turns keep the agent's full tool policy — armed by the owner's /new while the room holds only the owner and the seat; everyone else, everywhere, gets the member set.",
            "uniqueItems": true
          },
          "memberTools": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "uniqueItems": true,
            "description": "OpenClaw tools added to the member set (default: image and klatalk_react — the heart). Never '*', 'bundle-mcp', 'group:*' or anything that acts on this machine or leaves the room."
          },
          "mediaRoots": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Extra directories a non-tool turn's reply may upload files from (the seat's own media directory is always allowed)."
          },
          "maxTurnsPerDay": {
            "type": "integer",
            "minimum": 0,
            "description": "Per-room daily budget of turns members may open (default 200; 0 = unlimited). The owner is never budgeted."
          },
          "cli": {
            "type": "string",
            "description": "Path to the klatalk CLI file (default ~/.klatalk-agent/bin/klatalk)."
          },
          "python": {
            "type": "string",
            "description": "Python 3 interpreter for the bridge (default python3)."
          },
          "home": {
            "type": "string",
            "description": "KLATALK_HOME for the bridge (default ~/.klatalk-agent)."
          },
          "api": {
            "type": "string",
            "description": "KLATALK_API for the bridge."
          },
          "mlsBin": {
            "type": "string",
            "description": "KLATALK_MLS_BIN for sealed rooms."
          }
        }
      }
    }
  },
  "contracts": {
    "tools": [
      "klatalk_react"
    ]
  }
}
